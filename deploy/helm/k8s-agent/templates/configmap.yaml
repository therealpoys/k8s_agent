apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8s-agent.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
data:
  config.yaml: |
{{ .Values.agentConfig | toYaml | indent 4 }}
